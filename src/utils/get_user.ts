export default async function getUser(): Promise<string> {
  // replace with your database logic and return the current user ID
  // so you can display users sessions

  // You can use NextAuth easily here ;)

  return "example_user_id";
}
